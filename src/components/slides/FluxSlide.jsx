import React, { PropTypes } from 'react'
import flux from './flux.png'

const FluxSlide = () => (
  <div>
    <h3 className="ui inverted header">Что такое Redux?</h3>
    <h4 className="ui inverted header">Redux это Flux</h4>
    <p>
      <img src={flux} />
    </p>
  </div>
)

export default FluxSlide
