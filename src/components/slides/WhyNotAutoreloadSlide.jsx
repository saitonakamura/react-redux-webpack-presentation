import React, { PropTypes } from 'react'

const WhyNotAutoreloadSlide = () => (
    <div>
      Then why not auto reload?
      Write-Save-<i>autoreload</i>-Check
    </div>
)

export default WhyNotAutoreloadSlide
